# X short posts — Bridgent AI

> **Channel**: <https://x.com/>
> **Goal**: short single-post variants for the published
> `@bridgent/source-prisma@0.2.3` release.
> **Best window**: weekday 09:00–11:00 PT or 18:00–20:00 PT.

---

## Variant A — Problem first

```
Most teams already have OpenAPI specs, Prisma schemas, Drizzle tables, and typed functions.

Their AI agents still cannot see them because writing MCP servers is too much boilerplate.

Bridgent AI turns those existing definitions into MCP tools in one line.

https://js-mark.com/Bridgent/
```

## Variant B — Source list

```
Bridgent AI v0.2.3:

OpenAPI specs -> MCP tools
Prisma schemas -> safe read tools + audited writes
Drizzle tables -> read-only findMany
Zod functions -> typed custom tools

stdio, Streamable HTTP, and Web handlers from the same tool definitions.

https://js-mark.com/Bridgent/
```

## Variant C — Safety angle

```
Giving an AI agent database access should not mean giving it raw SQL.

Bridgent AI exposes Prisma safely:
row caps, query timeouts, raw SQL disabled, audited writes behind preview tokens, JSONL audit, and idempotent retries.

https://github.com/js-mark/bridgent
```

## Variant D — Builder ask

```
I am building Bridgent AI: one line to expose existing APIs, databases, and typed code as production-ready MCP servers.

What source should AI coding agents be able to see next: tRPC, GraphQL, or something else?

https://js-mark.com/Bridgent/
```

## Notes

- Pair one post with the logo lockup image.
- Reply with a code snippet rather than putting long code in the first post.
- Do not post all variants on the same day.
