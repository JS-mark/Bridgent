# v0.3 tRPC source — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship `@bridgent/source-trpc` as the v0.3 source-expansion feature and add the minimum source metadata needed for docs and future inspector work.

**Architecture:** Keep Bridgent explicit and source-file based. The tRPC adapter should generate `BridgentTool[]` from an imported router and execute procedures in-process through the tRPC caller API. Queries are exposed by default; mutations require explicit allowlisting; subscriptions are out of scope.

**Source spec:** `docs/superpowers/specs/2026-06-12-v0.3-trpc-source-design.md`.

## Task 1: Package Scaffold

- [ ] Add `packages/source-trpc`.
- [ ] Configure package metadata, exports, tsdown, tsconfig, lint/test/typecheck scripts.
- [ ] Use catalog dependencies only.
- [ ] Mark peer dependencies intentionally, especially tRPC and Zod-related peers.
- [ ] Add package to workspace/turbo flow automatically through existing globs.

## Task 2: Router Introspection

- [ ] Identify the stable tRPC v10/v11 router/procedure metadata available at runtime.
- [ ] Extract nested procedure paths.
- [ ] Classify procedures as query, mutation, or subscription.
- [ ] Apply `procedureFilter` before schema generation.
- [ ] Throw clear errors for unsupported router/procedure shapes.

## Task 3: Input Schema Mapping

- [ ] Map no-input procedures to an empty strict object schema.
- [ ] Map Zod-backed input parsers to Bridgent/MCP input schemas.
- [ ] Reject unsupported opaque parser shapes with actionable errors.
- [ ] Add tests for no-input, Zod object input, nested router paths, and unsupported input.

## Task 4: Tool Generation

- [ ] Implement `fromTrpc`.
- [ ] Generate stable tool names with optional `toolPrefix`.
- [ ] Detect and throw on name collisions.
- [ ] Execute procedures through the in-process caller API.
- [ ] Preserve existing Bridgent result behavior for strings and JSON-like values.
- [ ] Map thrown procedure errors consistently with existing source adapters.

## Task 5: Mutation Controls

- [ ] Keep mutations disabled by default.
- [ ] Require explicit mutating opt-in before exposing mutation procedures.
- [ ] Require a non-empty mutation allowlist.
- [ ] Document whether the allowlist uses final tool names or procedure paths.
- [ ] Add tests proving unallowlisted mutations are hidden and allowlisted mutations execute.

## Task 6: Source Metadata

- [ ] Add an additive metadata contract without breaking `BridgentTool[]`.
- [ ] Attach metadata from tRPC tools.
- [ ] Backfill lightweight metadata in existing adapters only where low-risk.
- [ ] Add tests for metadata shape.

## Task 7: Example

- [ ] Add `examples/06-trpc-router`.
- [ ] Include one query and one mutation.
- [ ] Default example exposes only the query.
- [ ] Add a documented allowlisted mutation variant.
- [ ] Ensure the example can run through `bridgent dev`.

## Task 8: Documentation

- [ ] Add `packages/source-trpc/README.md`.
- [ ] Add English VitePress `From tRPC`.
- [ ] Add Chinese VitePress `From tRPC`.
- [ ] Update source overview tables.
- [ ] Update getting-started next steps.
- [ ] Update root README when the package is ready to ship.
- [ ] Update changelog pages with the actual package version.
- [ ] Update `docs/progress.md`.

## Task 9: Validation

- [ ] Run focused source-trpc tests.
- [ ] Run docs lint/build.
- [ ] Run full gate: `pnpm turbo run build test typecheck lint`.
- [ ] Run package dry-run for `@bridgent/source-trpc`.

## Completion Notes

- Fill this section only after implementation and validation.
