# v0.2 CLI init — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add `bridgent init` as the first v0.2 onboarding feature while preserving Bridgent's explicit server-file model.

**Architecture:** Implement a narrow CLI command under `packages/cli/src/commands/init.ts` with pure helpers for file generation and filesystem behavior. Register it in `cli.ts`, test helpers with Vitest, and update docs to present init as an optional onboarding shortcut.

**Tech Stack:** TypeScript, citty, consola, Node `fs/promises`, Vitest, VitePress docs.

**Source spec:** `docs/superpowers/specs/2026-06-07-v0.2-cli-init-design.md`.

**Pre-flight:**
- Current baseline was committed as `d122d35 fix review issues and align roadmap`.
- Do not introduce a config file or source-adapter scaffold in this plan.
- Do not mutate package dependencies from the CLI.

---

## Task 1: Add Command Design Docs

- [x] Add `docs/superpowers/specs/2026-06-07-v0.2-cli-init-design.md`.
- [x] Add this implementation plan.
- [x] Keep task checkboxes updated as implementation progresses.

## Task 2: Implement CLI Command

- [x] Add `packages/cli/src/commands/init.ts`.
- [x] Generate a default `server.ts` when no file is provided.
- [x] Support a custom target path.
- [x] Create parent directories recursively.
- [x] Refuse to overwrite by default.
- [x] Support `--force` overwrite.
- [x] Print concise next-step commands.
- [x] Register `init` in `packages/cli/src/cli.ts`.

## Task 3: Add CLI Tests

- [x] Add Vitest coverage for generation helpers.
- [x] Cover default target.
- [x] Cover nested target directory creation.
- [x] Cover overwrite protection.
- [x] Cover `--force` overwrite.
- [x] Assert generated server imports and tool registration.

## Task 4: Update Docs

- [x] Update English getting-started docs with `bridgent init`.
- [x] Update Chinese getting-started docs with `bridgent init`.
- [x] Update roadmap to mark CLI init as in progress or shipped in v0.2.
- [x] Add dated progress log entry.

## Task 5: Validate

- [x] Run `pnpm --filter @bridgent/cli test`.
- [x] Run `pnpm --filter @bridgent/cli build`.
- [x] Run `pnpm --filter @bridgent/cli typecheck`.
- [x] Run docs lint/build.
- [x] Run the full Turbo gate if practical.

## Completion Notes

Implemented `bridgent init [file] [--force]`, added CLI unit coverage, updated public/internal docs, added a changeset, and validated with `pnpm turbo run build test typecheck lint`.
