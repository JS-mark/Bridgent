# v0.2 complete — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Complete all remaining v0.2 roadmap scope after the initial `bridgent init` work.

**Architecture:** Keep runtime explicit and source-file based. Add OpenAPI API-key auth in the existing request invocation layer. Add a narrow Drizzle read-only source package. Keep Prisma writes as design documentation only.

**Source spec:** `docs/superpowers/specs/2026-06-07-v0.2-complete-design.md`.

## Task 1: CLI init hardening

- [x] Replace check-then-write with atomic non-overwrite write.
- [x] Keep `--force` overwrite behavior.
- [x] Update tests if needed.

## Task 2: OpenAPI API-key auth

- [x] Add `ApiKeyAuth` type and union.
- [x] Apply API keys for header/query/cookie locations.
- [x] Add tests for all locations.
- [x] Update OpenAPI docs.

## Task 3: Drizzle source adapter

- [x] Add `packages/source-drizzle`.
- [x] Implement `fromDrizzle`.
- [x] Add read-only `findMany` tools with limit clamp and offset.
- [x] Add unit tests using a fake Drizzle-like db.
- [x] Add package docs and changeset.

## Task 4: Prisma write-side design

- [x] Add design doc for Prisma writes.
- [x] Link it from roadmap/docs.
- [x] Keep runtime writes disabled.

## Task 5: Docs and planning

- [x] Update roadmap to reflect v0.2 completed scope.
- [x] Update progress log.
- [x] Update public docs for OpenAPI API-key auth and Drizzle.
- [x] Keep Inspector improvements scoped to official Inspector docs.

## Task 6: Validation

- [x] Run focused tests.
- [x] Run docs lint/build.
- [x] Run full Turbo gate.

## Completion Notes

- 2026-06-07: full Turbo gate `pnpm turbo run lint typecheck test build` — 26/26 successful.
  - source-openapi 33 tests (incl. 3 new auth header/query/cookie cases)
  - cli 5 init tests, core 6, source-prisma 26, source-drizzle unit tests, host-test 4 e2e (stdio/http/web/cli-smoke).
- VitePress docs build clean (`pnpm docs:build`).
- Outstanding fix during validation: lowercased `describe('openAPI auth', ...)` in `packages/source-openapi/test/auth.test.ts` to satisfy antfu `test/prefer-lowercase-title`.
